# Built CLI binary from `go build ./cmd/skills`
/skills

# Local runtime state if this repo uses the tool itself
/.agents/state.yaml
/.agents/skills/
/.agents/cache/
/.claude/skills/

# Common local test output
/coverage.out
*.test

# macOS filesystem noise
.DS_Store
